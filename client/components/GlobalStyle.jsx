import { createGlobalStyle } from 'styled-components';

const GlobalStyle = createGlobalStyle`
  body {
    background: linear-gradient(10deg, rgba(137,196,227,1) 0%, rgba(203,142,144,1) 100%);
    font-family: 'Futura';
    color: white;
  }

`

export default GlobalStyle;
